<article>
  <header>
    <h1>
      <a href="<%= url('articles/' + article.slug) %>"><%= article.title %></a>
    </h1>
    <time class="timeago" datetime="<%= article.date.xmlschema %>">
      <%= article.date.strftime "%Y/%m/%d" %>
    </time>
  </header>
  <section class="content">
    <%= truncator_markdown(article.content, 1) %>
  </section>
</article>